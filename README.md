# MiddleMan
MiddleMan is a standalone, US-only bill consolidation service: users connect their subscriptions, MiddleMan puts a unique virtual card on each provider, pays every provider as charges hit, then issues the user one consolidated invoice per month 
